import Ship from "./Ship";

const Player = (ai) => {
  let previousShots = [];

  // Create Ships
  let carrier = new Ship("Carrier", 5);
  let battleship = new Ship("Battleship", 4);
  let cruiser = new Ship("Cruiser", 5);
  let submarine = new Ship("Carrier", 5);
  let destroyer = new Ship("Carrier", 5);

  let ships = [];
};

export default Player;
